require("dotenv").config();

module.exports = {
    TOKEN: process.env.TOKEN || "",  // bot token gir :)
    PREFIX: process.env.PREFIX || "!", // bot prefix
    SAHİP: process.env.SAHİP || "822770318740553729", //sahip id :()
}